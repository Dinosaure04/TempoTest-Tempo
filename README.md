# TempoTest-Tempo
Minimal TempoTest smart contract deployed on Tempo Testnet
# TempoTest-Tempo

Minimal Solidity contract **TempoTest** deployed on the **Tempo Testnet**.

## Description

`TempoTest` is a simple contract that allows you to:

- Store a number (`value`)  
- Modify this value using the function `setValue(uint256 _v)`  

It’s a perfect example for testing deployment on the Tempo Testnet and interacting with a simple smart contract.
